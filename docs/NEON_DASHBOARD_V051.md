# Neon Dashboard v0.5.1

This revision applies the user-selected blue/purple/green neon dashboard to the real app.

The screenshot concept is treated as a visual direction only. Production UI uses original Compose code and Material icons; no third-party cleaner UI source code or layout is copied.

The Pro badge from the concept is intentionally omitted because no paid product exists.

See `docs/HOME_DESIGN.md` for the exact functional mapping.
